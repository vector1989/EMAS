/**
 * 添加，上传图片 插件
 */
KindEditor.plugin('imgupload', function(K) {
        var editor = this, name = 'imgupload';
        // 点击图标时执行
        editor.clickToolbar(name, function() {
        	EditArticle.source(1,'图片库');
        });
});